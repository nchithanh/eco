# Dolphin Edu frontend (this repo)

Standalone **Agent CRM** UI for learning-center booking. Hardcoded FE — no API.

## For AI / agents

1. Read `context/` (product SoT) and `documentations/` before inventing behavior.
2. **Always** update `documentations/` **and** parent `../context/` in the **same task** when code **or** requirements change.
3. Do not use Dolphin Ops spa domain or a marketing site as app spec.
4. Confirm-before-acting when that workflow is enabled. Missing price / SLA → **TODO**.

## Humans

Start with root `README.md` (setup + folder map).
