# Quarantine — spa leftovers

Không import từ app Dolphin Edu. Giữ để tham chiếu khi cần; UI live dùng bảng CRM (`EduTable`) + domain Course → Class.

| File | Lý do |
| --- | --- |
| `BookingForm` | Booking spa 1:1 slot |
| `TodayList` | Danh sách booking hôm nay |
| `Inbox` / `TaskList` | Nav `disabled`, không canvas |
| `ApprovalPanel` | Duyệt task spa |
| `PageToolbar` | Toolbar list spa (stub đã dùng `EduTable` tools) |
