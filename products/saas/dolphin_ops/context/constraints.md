# Constraints

- Missing price, SLA, KPI → write **TODO**. Never invent numbers or cases.
- Do not claim the Agent runtime is live while this repo is still a scaffold.
- Backend (`backend/`) is CRM only (Go + Postgres). Do not add Agent/tool/intent code there.
- Do not load marketing homepage / `dolphin-ops-copy.ts` as implementation spec.
- Brand display **Dolphin Software**; tokens from `frontend/app/tokens.css`.
- Human stays in control on sensitive actions.
- Cookie-consent bump applies to the **marketing** Pages site (`lib/cookie-consent.ts`), not this app, unless Ops ships its own banner later.
