# Products

SaaS and standalone apps **outside** the marketing site (repo root).

| Path | Product | Host (planned) | GitHub |
| --- | --- | --- | --- |
| `saas/dolphin_ops/` | Dolphin Ops — Agent CRM | `https://dolphin-ops.dolphin-software.io.vn` | FE: `nchithanh/dolphinOps` · BE: `nchithanh/dolphinOpsBackend` (private) |

## Agent rules

- Coding a product → read **that product folder only** (`AGENTS.md` + `context/`). Do not use marketing `.cursor/knowledge/` or `documentations/homepage.md` as product spec.
- Skill: `dolphin-ops` when the task is the Ops app.
- Brand / `--kuct-*` changes at repo root → update `products/_shared/brand/` and each product copy (see `_shared/brand/README.md`).

Marketing landing for Ops stays at `/dolphin-ops/` on the Pages site.
