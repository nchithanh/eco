/** Inline boot scripts — injected via `BootScripts` + `useServerInsertedHTML` (outside React tree). */

export const THEME_BOOT_SCRIPT = `(function(){try{var t=localStorage.getItem("kuct-theme");var ok=["violet","slate"];if(t&&ok.indexOf(t)>=0)document.documentElement.setAttribute("data-theme",t);else document.documentElement.setAttribute("data-theme","violet");}catch(e){document.documentElement.setAttribute("data-theme","violet");}})();`;

export const LOCALE_BOOT_SCRIPT = `(function(){var d=document.documentElement;try{d.setAttribute("data-locale-pending","");var s=document.createElement("style");s.id="kuct-locale-boot";s.textContent="html[data-locale-pending],html[data-locale-pending] body{visibility:hidden!important}";document.head.appendChild(s);var ok=["vi","en","ja"];var locale=null;try{var stored=localStorage.getItem("kuct-locale");if(stored&&ok.indexOf(stored)>=0)locale=stored;}catch(e){}if(!locale){var langs=(navigator.languages&&navigator.languages.length)?navigator.languages:[navigator.language];for(var i=0;i<langs.length;i++){var p=String(langs[i]||"").toLowerCase().split("-")[0];if(ok.indexOf(p)>=0){locale=p;break;}}}if(!locale)locale="en";d.lang=locale;d.setAttribute("data-locale",locale);}catch(e){d.lang="en";d.setAttribute("data-locale","en");d.removeAttribute("data-locale-pending");var b=document.getElementById("kuct-locale-boot");if(b)b.remove();}})();`;
