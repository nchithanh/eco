import type { Locale } from "./types";

/** Utility / footer label for Templates → `/demos/`. */
export const templatesNavLabel: Record<Locale, string> = {
  vi: "Templates",
  en: "Templates",
  ja: "テンプレート",
};
